import React from "react";

const ChatInput = () => {
  return (
    <>
      <input type="text" placeholder="Enter your message" />
      <button>Send</button>
    </>
  );
};

export default ChatInput;
